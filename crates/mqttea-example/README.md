# mqttea-example

An example CLI tool to test mqttea functionality.

## Usage

1. Start up `mosquitto` (or some MQTT broker) locally.
2. In one tab, run `./mqttea-example --listen`.
3. In another tab, run `./mqttea-example`.

Watch the "listener" tab receive and process messages from the "sender" tab.

Woohoo!
